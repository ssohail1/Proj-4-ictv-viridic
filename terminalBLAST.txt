# commands to download and install current version of BLAST for linux server

sudo apt-get update
sudo apt-get install ncbi-blast+

# to check that it is correctly installed type

blastn help

Run Commands:
makeblastdb -in fastaseq.fasta -dbtype nucl -out customdb.fasta
blastn -db fastaseq.fasta -query myfile1.fasta
